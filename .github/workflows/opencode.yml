# OpenCode — Issue 触发自动改代码
# 模型：gpt-5.2 | 思考：high
name: OpenCode Agent

# 已暂停 OpenCode（主人决定先只用 Codex）
# 如需手动跑：在 GitHub Actions 里手动 workflow_dispatch 触发
on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  issues: write
  actions: write

jobs:
  opencode:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    if: "!contains(github.event.issue.labels.*.name, 'skip-opencode')"
    steps:
      - uses: actions/checkout@v4

      - name: Install OpenCode
        run: |
          curl -fsSL https://opencode.ai/install | bash
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Run OpenCode
        env:
          OPENAI_API_KEY: ${{ secrets.OPENCODE_API_KEY }}
          OPENAI_API_BASE: ${{ secrets.OPENCODE_API_BASE }}
        run: |
          opencode run \
            --model openai/gpt-5.2 \
            --variant high \
            "根据以下需求修改代码：
          标题：${{ github.event.issue.title }}
          内容：${{ github.event.issue.body }}"

      - name: Create PR
        id: pr
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: opencode/issue-${{ github.event.issue.number }}
          title: "🛠️ OpenCode: ${{ github.event.issue.title }}"
          body: |
            由 OpenCode (gpt-5.2, thinking: high) 自动生成
            对应 Issue #${{ github.event.issue.number }}
          commit-message: "feat: opencode auto-fix #${{ github.event.issue.number }}"
          labels: agent-opencode
