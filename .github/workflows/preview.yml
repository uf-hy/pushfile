# PR 预览部署 — 在闲置服务器(de)上起 Docker 预览
# 使用受限 SSH Key，只能执行部署脚本，无法登录 shell
name: Preview Deploy

on:
  pull_request:
    types: [opened, synchronize, reopened, closed]

permissions:
  pull-requests: write

jobs:
  deploy-preview:
    runs-on: ubuntu-latest
    if: github.event.action != 'closed'
    steps:
      - name: Deploy Preview
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_SSH_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_SSH_PORT }}
          script: |
            deploy ${{ github.event.pull_request.number }} ${{ github.head_ref }}

      - name: Comment Preview URL
        uses: actions/github-script@v7
        with:
          script: |
            const prNum = context.payload.pull_request.number;
            const port = 35000 + prNum;
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNum,
              body: `🚀 **预览环境已部署**\n\n` +
                `- 预览地址: http://${{ secrets.SERVER_HOST }}:${port}/\n` +
                `- 预览密码: \`preview-test-key\`\n\n` +
                `每次推送代码会自动更新预览。PR 关闭后自动销毁。`
            });

  cleanup-preview:
    runs-on: ubuntu-latest
    if: github.event.action == 'closed'
    steps:
      - name: Cleanup Preview
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_SSH_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_SSH_PORT }}
          script: |
            cleanup ${{ github.event.pull_request.number }}
