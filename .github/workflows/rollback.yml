# 手动回滚 — 恢复到指定版本
name: Rollback

on:
  workflow_dispatch:
    inputs:
      commit:
        description: '目标 commit hash（留空则回滚到上一个版本）'
        required: false
        default: ''

jobs:
  rollback:
    runs-on: ubuntu-latest
    steps:
      - name: Rollback
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.PROD_SERVER_HOST }}
          username: ${{ secrets.PROD_SERVER_USER }}
          key: ${{ secrets.PROD_SERVER_SSH_KEY }}
          port: ${{ secrets.PROD_SERVER_SSH_PORT }}
          script: |
            rollback-prod ${{ github.event.inputs.commit }}

      - name: Report result
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const commit = '${{ github.event.inputs.commit }}' || 'HEAD~1';
            const status = '${{ job.status }}' === 'success' ? '✅' : '❌';
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `${status} 回滚到 ${commit}`,
              body: `回滚操作 ${status === '✅' ? '成功' : '失败'}\n\n目标: ${commit}\nWorkflow: ${context.runId}`,
              labels: ['rollback'],
            });
